<%= semantic_form_for [:admin, @photo] do |f| %>
<% f.inputs do %>
  <%= f.inputs :file %>
    <%= f.inputs :name, input_html: {:class => "tinymce"} %>
  <%= f.actions :submit %>
  <%= tinymce %>
<% end %>
<% end %>